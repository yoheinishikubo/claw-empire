# Releases

- [`v1.2.3`](v1.2.3.md) - Direct messenger integration/stabilization release: unified built-in channel catalog + native adapters, Settings chat-session UX redesign (add/edit modal + immediate persist + agent avatar mapping + helper guidance), task-scoped channel relay isolation for report/chat/status_update, Decision Inbox in-messenger numeric reply flow with duplicate-send guard and compact formatting, messenger completion-report summary formatting with agent-identity intro, plus project-binding/path-safety hardening (`PROJECT_PATH_ALLOWED_ROOTS`) and direct-chat reliability/type cleanup
- [`v1.2.2`](v1.2.2.md) - Interrupt-inject workflow rollout (new paused-session prompt injection endpoint + session-proof token + CSRF guard + terminal interrupt UX), plus Office Agent Detail per-agent CLI model overrides (`cli_model`) and Codex reasoning-effort override (`cli_reasoning_level`) propagated through task run/orchestration/spawn paths; includes schema/type expansion, API docs update for `/api/tasks/:id/inject`, light-mode interrupt contrast polish, and added interrupt control/injection tests
- [`v1.2.1`](v1.2.1.md) - Post-v1.2.0 hardening release: core task-route split hotfix, CI Trojan Source (hidden/bidi Unicode) guard, pnpm version-mismatch fix, explicit typecheck/build gates with least-privilege workflow permissions, ESLint flat-config + CI lint baseline, staged incremental lint tightening (`lint-staged`), RuntimeContext/type-debt reduction (including Decision Inbox type dedupe), test-runtime DB isolation hardening, Swagger/OpenAPI docs exposure, OpenAPI contract automation (`openapi:sync`/`openapi:check` + CI gate + standard error/example coverage), and multilingual README/security/API docs refresh
- [`v1.2.0`](v1.2.0.md) - Agent management CRUD (hire/edit/delete with full multilingual support), department CRUD (create/edit/delete with color/icon/prompt), department management tab with arrow/drag-and-drop sort order editing, new pixel character sprite (#13), project-level manual agent assignment with sprite avatar multi-select, meeting participant filtering for manual-mode projects (planning leader + assigned agents' department leaders only), task delegation manual-mode candidate restriction (assigned pool + same leader department), ChatPanel manual-mode indicator, mobile-responsive Project Manager (list-detail toggle), custom skill upload system (upload/list/delete with classroom training animation and light-mode support), bug fixes for runInTransaction 500 error, `/api/departments/reorder` route-collision save failure, light-mode error message contrast, and Agent Manager modal/emoji overflow scroll issues, plus post-release hardening kept in v1.2.0: manual-assignment safeguard confirmations/stats, backend `agent_ids` validation, delegation fallback audit/CEO notice, sprite registration collision guard, portable sprite generation output path, and department sort_order UNIQUE index safe migration
- [`v1.1.9`](v1.1.9.md) - Post-v1.1.8 reliability hotfix bundle: sub-agent status-sync hardening (unknown `agent_status` live-sync fallback + Codex thread-binding TTL/size pruning and done-time cleanup), delegated review-gate pause/resume fixes (no false blocked finalization on pause interrupts, completion-time subtask reconciliation, stale blocked delegation auto-heal during review completion), and Decision Inbox review-round skip routing fix (runtime binding + rollback guard to prevent false project-level fallback)
- [`v1.1.8`](v1.1.8.md) - Sub-agent orchestration reliability hardening across Claude/Codex/OpenCode/Gemini stream formats, burst-safe CLI output tail buffering for sub-agent UI visibility, equality/sync guardrails to reduce stale frames and ghost-agent artifacts, plus Office sub-agent visual refresh (random helper sprites, calmer support motion, smoke summon/despawn + fireworks) and Agent Detail sub-agent tab sprite-icon sync
- [`v1.1.7`](v1.1.7.md) - OpenCode/Kimi stream-output hardening (final-reply extraction, raw JSON leak suppression, terminal-pretty reasoning visibility restore, explicit permission/stale-file/tool-calls/timeout error replies, improved OpenCode tool-progress hint stability), plus post-v1.1.6 hotfixes for custom API non-`v1` version-path compatibility and setup frontend URL port correction (`5173 -> 8800`)
- [`v1.1.6`](v1.1.6.md) - GitHub project integration: dev-branch merge + automatic PR creation for GitHub-imported projects, `github_repo` column/API support, GitHub Import bugfixes, plus post-release hardening for subtask batch delegation worktree coverage across providers, shared HTTP fake-PID allocation, delegated-cancel queue safety, and pre-merge auto-commit policy/error classification improvements
- [`v1.1.5`](v1.1.5.md) - Task/project creation UX overhaul: project-name input + click dropdown, inline new-project branch and one-flow project+task creation, desktop 1:1 expand + mobile scroll + outside-click close guard, latest-3 draft recovery submodals, project-path helper suite (auto/in-app/native picker) across Task + Project Manager, plus post-release hardening for helper API 404 compatibility, in-modal error UX, headless native-picker handling, duplicate `project_path` conflict prevention, allowed-root enforcement, backend `project_path` mapping fallback, and CLI spawn `ENOENT` resilience via cross-platform `PATH` fallback
- [`v1.1.4`](v1.1.4.md) - Added global Auto Update toggle in Settings (server-wide), runtime toggle-apply endpoint (`/api/update-auto-config`), legacy upgrade migration keys (`autoUpdateEnabled`, `autoUpdateNoticePending`) with one-time notice for upgraded `v1.1.3` users, and fail-closed guard hardening with non-overridable `channel_check_unavailable`; updated multilingual READMEs and release docs
- [`v1.1.3`](v1.1.3.md) - Project-first directive/task flow (existing/new project branch + top-10 selection), Project Manager rollout with mapped task/report history, task-detail report modal improvements, strict OpenClaw AGENTS upgrade enforcement (`HTTP 428 agent_upgrade_required`), light-mode Project Manager button contrast fix, browser stability optimization (WebSocket broadcast batching, tab-hidden polling pause, Pixi.js GPU memory cleanup, state array GC reduction, ChatPanel memoization), task creation agent assignment fix, run-guard UX with shake animation, dashboard header button primary/secondary redesign, and PR #23 (`SJY0917032`) follow-up for meeting-prompt/token compaction defaults (`MEETING_TRANSCRIPT_MAX_TURNS=20`) plus first-run `.env` auto-seeding for missing meeting prompt keys; added Active Agents `Script`/`Idle CLI` process-check menu with sample images
- [`v1.1.2`](v1.1.2.md) - Dark/light theme-system rollout with tokenized UI surfaces, office scene palette adaptation, dashboard/sidebar/terminal readability polish, and light-mode learn/unlearn modal contrast fixes (including text-only note for updated Office screenshot asset)
- [`v1.1.1`](v1.1.1.md) - Office Manager rollout (department/CEO/break-room theme control + live room highlight), tone-first preset behavior, report document pagination, DB-backed room-theme defaults/persistence, plus hotfixes for false `in_progress -> inbox` watchdog recovery and `/api/tasks` request burst throttling
- [`v1.1.0`](v1.1.0.md) - PR #19 integration and release consolidation for Task Board bulk hide controls (Done/Pending/Cancelled), Active/All visibility toggle, Skills Learn/Unlearn UX upgrades, cross-platform unlearn verification, and post-release in-app update banner guidance
- [`v1.0.9`](v1.0.9.md) - Report-request/PPT workflow hardening (HTML-first + one-pass design checkpoint + final regen/no second review), terminal live-hint UX and parser improvements, task log append retention, and multilingual README/report-doc updates
- [`v1.0.8`](v1.0.8.md) - Active agents status panel + kill control, task completion report popup/history, planning-lead consolidated archive generation, long-run stability updates (`--max-turns 200`, timeout/orphan handling), and CLI/OAuth/API communication QA test suite
- [`v1.0.7`](v1.0.7.md) - External API provider system (Settings API tab, CRUD + connection test), direct chat streaming for API/OAuth agents (`chat_stream` WS event), API provider task execution, `orchestration.ts` const crash fix, model caching layer (SQLite + memory 2-tier)
- [`v1.0.6`](v1.0.6.md) - Windows cross-platform compatibility (CLI detection `shell: true`, Gemini version detection, skill-learn `spawn` fix), OAuth model provider independence (Copilot API direct fetch → 3-tier resolution), Settings panel version-display fallback, non-interactive esbuild build approval
- [`v1.0.5`](v1.0.5.md) - Runtime modularization follow-up (route/workflow decomposition), `/api/inbox` security-doc parity (`x-inbox-secret`), one-time post-pull auto-migration (`INBOX_WEBHOOK_SECRET`/AGENTS/OpenClaw path), AGENTS secret-discovery hardening, runtime language-alignment fixes, long chat-response truncation/repetition fixes, workflow crash hotfix, and Skills Library Learn Squad background install flow (`npx skills add ...`)
- [`v1.0.4`](v1.0.4.md) - Review flow stabilization (R1 single remediation batch -> R2 merge synthesis -> R3 final), planning-leader subtask rerouting, department-batched sequential checklist delegation, collaboration child review-gate + single parent finalization, orphan in-progress watchdog recovery, explicit remediation/collaboration reporting, and pause/resume session continuity hardening
- [`v1.0.3`](v1.0.3.md) - Message delivery resilience hardening (idempotency/retry/audit verification) + review workflow guardrails for loop control
- [`v1.0.2`](v1.0.2.md) - Agent token optimization: auto project context, recent changes sharing, CLAUDE.md generation, shared MVP code review policy + explicit warning-fix override
- [`v1.0.1`](v1.0.1.md) - OAuth multi-account pool, Office account routing, direct chat-to-task execution, mobile virtual controls
